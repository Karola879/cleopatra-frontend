export interface Customer {
    customerId: number;
    identityUserId: string;
    name: string;
    email: string;
    phoneNumber: string;
    dateOfBirth: Date;
    profilePicture: string;
    createdDate: Date
}